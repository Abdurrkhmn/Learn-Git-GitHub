1. Create new folder
In terminal: mkdir
2. Initiate git Inside folder learnGit 
In terminal: git init
3. Create random file
In terminal: touch index.js
3. Check file status
In terminal: git status
4. Add file to staging
In terminal: git add .
5. Committing the staging file
In terminal: git commit -m "Your Commit Message,
determines your changes"

untuk memperbarui
the first time STAGING (git add . /file),  second time STATUS (git status),  then commit and push